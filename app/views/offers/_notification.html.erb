<div class="notification-container">
  <div class="alert alert-success alert-dismissible fade show" role="alert">
    <strong><%= user.first_name.capitalize %> <% user.last_name.capitalize %> vient de rejoindre ton projet ! 🎉 </strong>
    <strong> <%= user.offers.last.quantity %> <%= user.offers.last.product.product_type  %>s </strong> supplémentaires !
    <p><strong>Voici son message :</strong></p>
    <p><em>" <%= user.offers.last.description  %> "</p></em>
      <button type="button" class="close" data-dismiss="alert" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
  </div>
</div>
